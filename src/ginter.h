#ifndef _GINTER_H
#define _GINTER_H

#include <map>
#include "index.h"
#include "par.h"

#include "binmap.h"
#include "index.h"

using namespace std;

void parse_ginteraction(const char *input_name, BINMAP &binmap, INDEX &index);

#endif /* _BINMAP_H */